<%= content_for :javascript do %>
  <%= javascript_include_tag "cambiar_pass", "data-turbolinks-track" => false %>
<% end -%>
<div class="row">
  <div class="col-sm-6 col-sm-offset-3 form-box">
    <div class="form-top">
      <div class="form-top-left">
        <h3>Introduzca su nueva contraseña</h3>
      </div>
      <div class="form-top-right">
        <i class="fa fa-lock"></i>
      </div>
    </div>
    <%= form_for(current_user, url: change_pass_path, html: {id: 'change_pass', format: :json}) do |f| %>

      <div class="form-group">
        <%= f.password_field :password, autofocus: true, class: 'form-username form-control', placeholder: 'Nueva contraseña' %>
      </div>

      <div class="form-group">
        <%= f.label :confirmar_contraseña %><br />
        <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-username form-control', placeholder: 'Confirmar contraseña' %>
      </div>
      
      <div class="actions text-center">
        <%= f.submit 'Cambiar', class: 'btn btn-primary' %>
      </div>

    <% end %>
  </div>
</div>